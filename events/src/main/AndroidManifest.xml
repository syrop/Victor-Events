<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="pl.org.seva.events">

    <application
        android:name=".EventsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.EventsActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.CreateEventActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".view.activity.LoginQuestionActivity"
            android:label="@string/activity_create_community" />
        <activity
            android:name=".view.activity.LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".view.activity.AddCommActivity"
            android:label="@string/app_name" />

    </application>
</manifest>
